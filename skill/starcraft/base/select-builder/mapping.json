{
  "label": "select builder",
  "goal": [
    "build a cybernetics core",
    "build a forge",
    "build a gateway",
    "build a pylon"
  ],

  "comment": "Selects a builder close to the location",

  "body": {
    "is-game": true
  },

  "memory": {
    "nodes": {
      "LOCATION": { "type": "location", "purpose": "build" },
      "PROBE": { "unitType": "probe" }
    },

    "paths": [
      { "path": ["PROBE"] },
      { "path": ["GOAL", "location", "LOCATION"] },
      {
        "label": "SELECT",
        "path": ["GOAL", "builder", "PROBE"],
        "provisional": true,
        "unique": true
      }
    ]
  },

  "input": [
    "LOCATION/x",
    "LOCATION/y",
    "PROBE/busy",
    "PROBE/x",
    "PROBE/y"
  ],

  "output": [
    "SELECT"
  ]
}
