
export default class UseChronoBoostBrain {

  react(input) {
    const nexus = input[0];
    const countOfProbes = input[1];

    if (countOfProbes < 20) {
      return [nexus, 3755, nexus];
    }
  }

}
