{
  "label": "select builder nexus",
  "goal": "build a nexus",

  "body": {
    "is-game": true
  },

  "memory": {
    "nodes": {
      "DIRECTION": { "type": "resources" },
      "BUILDER": { "unitType": "probe" },
      "PROBE": { "unitType": "probe" }
    },

    "paths": [
      { "path": ["PROBE"] },
      { "path": ["GOAL", "direction", "DIRECTION"] },
      {
        "path": ["GOAL", "builder", "BUILDER"],
        "optional": true
      },
      {
        "label": "SELECT-PROBE",
        "path": ["GOAL", "builder", "PROBE"],
        "provisional": true,
        "unique": true
      }
    ]
  },

  "input": [
    "BUILDER",
    "PROBE/busy"
  ],

  "output": [
    "SELECT-PROBE"
  ]
}
